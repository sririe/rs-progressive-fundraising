# CLAUDE.md

> **Shim — not a source of truth.** This repository is agent- and lineage-agnostic.
> All guidance lives in **[AGENTS.md](AGENTS.md)** — read that.
>
> This file exists only because Claude tooling auto-loads `CLAUDE.md`. **Do not add project
> content here** (it drifts from `AGENTS.md` and the duplication has to be cleaned up every
> time). If you're tempted to write here, edit `AGENTS.md` instead.
